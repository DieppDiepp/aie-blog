# Copy this file to .env.prod ON THE VPS and fill in real values.
# The real .env.prod is git-ignored and never leaves the VPS.

# PostgreSQL: use a strong password in production, not the dev default.
POSTGRES_USER=aie
POSTGRES_PASSWORD=CHANGE_ME_STRONG
POSTGRES_DB=aie_blog

# Backend. Must match the POSTGRES_* values above (host is the compose service "db").
DATABASE_URL=postgresql+psycopg://aie:CHANGE_ME_STRONG@db:5432/aie_blog
# The public origin the site is served from, for CORS. With a named tunnel this
# is the real, stable domain. Only matters once the browser calls the api
# directly (the site is static for now), but keep it correct.
FRONTEND_ORIGIN=https://aiengineerblog.com

# Frontend: internal address of the api inside the docker network.
API_URL=http://api:8000

# Which image tag to run. The deploy step overrides this with the git SHA;
# leave as latest for a manual "run whatever is newest" pull.
IMAGE_TAG=latest

# Named tunnel credential. Create the tunnel in the Cloudflare Zero Trust
# dashboard (Networks > Tunnels), copy the token from the Docker install command
# (the long eyJ... string after --token), and paste it here. This is a SECRET:
# it authenticates the connector to your tunnel. Never commit the real value.
TUNNEL_TOKEN=CHANGE_ME_TUNNEL_TOKEN
